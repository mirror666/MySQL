第一章 帮助文档的使用

MYSQL有许多不同的版本，很多特性和语法也可能不一样。
这时，我们可以用帮助命令来查看当前版本的语法。

查看常用命令：      ?;
                   help;
                   \h;              三种都可以

查看帮助文档目录：  ? contents;

查阅语法：         ? 关键字;
                  eg: ? show;
                      ? create table;
